# Active Context

## Current Phase
Initial Setup and Tool Creation

## Recent Changes
- Initialized workspace with God Mode AI OS.
- Created `index.html` featuring a split-pane live preview tool powered by `mermaid.js`.
- Created `run.py` to serve the tool locally and automatically open it in the default browser.

## Next Steps
- Run the tool (`python3 run.py`) to test functionality.
- Close out the session and perform Git commit.
